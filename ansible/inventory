localhost ansible_connection=local

# These are the machines of example.com that will need ansible playbooks to
# get started.  Once the chef server is up and running, we can manage machines
# with that.
buildbox.example.com
router.example.com
repomirror.example.com
chefserver.example.com
chefworkstation.example.com ansible_connection=local
