---
- name: Ensure firewall permits http traffic
  firewalld:
    service: "{{ item }}"
    zone: public
    state: enabled
    immediate: yes
    permanent: yes
  with_items:
    - http
    - https
  become: yes
